"""Application configuration for the web_site app."""

from django.apps import AppConfig


class WebSiteConfig(AppConfig):
    """Configuration for the web_site application."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "src.web_site"
